name: Hub joblib quarantine PR

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  quarantine:
    name: Open Hub PR deleting model.joblib
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Check out exact source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: '3.12.13'

      - name: Install Hub client
        run: python -m pip install --disable-pip-version-check "huggingface_hub==1.23.0"

      - name: Open Hub PR at exact parent_commit
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN || secrets.HF_ORG_TOKEN }}
          HUB_REPO_ID: SZLHOLDINGS/szl-provctl
        run: python scripts/hub_quarantine_joblib.py
