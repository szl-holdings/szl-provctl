[project]
name = "szl-provctl"
version = "0.1.0"
description = "Provenance-DAG control and in-toto / SLSA interop. Software kernel, not a trained model."
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
